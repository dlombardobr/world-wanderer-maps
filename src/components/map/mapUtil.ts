
// This file is now just a proxy to maintain backwards compatibility
// It re-exports all utilities from the new files

import { 
  getPortName,
  getPort,
  getPortIcon,
  updateMapStyle,
  addMapStyles,
  filterRoutes,
  getRouteColor,
  zoomToRegion
} from './utils';

export {
  getPortName,
  getPort,
  getPortIcon,
  updateMapStyle,
  addMapStyles,
  filterRoutes,
  getRouteColor,
  zoomToRegion
};
